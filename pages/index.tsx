import type { NextPage } from "next";
import Nav from "../components/Nav";
const Home: NextPage = () => {
  return (
    <div className="h-full">
      <Nav />
    </div>
  );
};

export default Home;
